export interface loginCredential {
    username: string;
    passowrd: string;
}

export interface product {
    title: string;
    price: number;
    description: string;
    image: string;
    category: string;
}